
title: SSH Failed Login Detection

status: experimental

logsource:
  product: linux

detection:

  selection:
    message|contains:
      - "Failed password"

  condition:
    selection

level:
  high
