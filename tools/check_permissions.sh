#!/bin/bash

# ==========================================================
# الهدف:
# التحقق من صلاحيات ملفات المشروع.
# ==========================================================

find . -type f -name "*.sh" -exec ls -l {} \;

echo

find . -type f -name "*.py" -exec ls -l {} \;
